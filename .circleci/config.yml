# This config was automatically generated from your source code
# Stacks detected: deps:python:webServer/backend
version: 2.1
orbs:
  python: circleci/python@2

jobs:
  test-app:
    docker:
      - image: cimg/python:3.12.3
    working_directory: ~/project/Senior-Design-Project-L3M/app
    steps:
      - checkout:
          path: ~/project
          fetch-depth: 0
      - run:
          name: Debug Directory Structure
          command: ls -R ~/project/Senior-Design-Project-L3M/
      - run:
          name: Install dependencies
          command: |
            pip install --upgrade pip
            pip install -r requirements.txt
      - run:
          name: Run tests
          command: pytest test/ --junitxml=test-results/junit.xml || ((($? == 5)) && echo 'Did not find any tests to run.'))
      - store_test_results:
          path: test-results

#  deploy:
     #This is an example deploy job, not actually used by the workflow
#    docker:
#      - image: cimg/base:stable
#    steps:
      # Replace this with steps to deploy to users
#      - run:
#          name: deploy
#          command: '#e.g. ./deploy.sh'

workflows:
  build-and-test:
    jobs:
      - test-app
    # - deploy:
    #     requires:
    #       - test-python
